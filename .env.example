# ══════════════════════════════════════════════════════════════════════
# Demo Mode (set both to 'true' to run without any credentials)
# ══════════════════════════════════════════════════════════════════════

# DEMO_MODE=true
# VITE_DEMO_MODE=true

# ══════════════════════════════════════════════════════════════════════
# Platform Configuration (non-secret settings)
# ══════════════════════════════════════════════════════════════════════

ADMIN_EMAILS=you@redhat.com
JIRA_HOST=https://redhat.atlassian.net

# Auth email domain override (optional)
# If your OAuth proxy rewrites user emails to a different domain (e.g. cluster.local),
# set this so role assignments match the authenticated email domain.
# AUTH_EMAIL_DOMAIN=

# API Endpoint (optional — defaults to /api, which Vite proxies to the dev server on port 3001)
# Use a full URL only if the UI and API are on different origins.
# VITE_API_ENDPOINT=

# Local dev server port (optional - defaults to 3001)
# API_PORT=3001

# Proxy auth secret (optional - when set, backend requires X-Proxy-Secret header)
# Used in production: nginx injects this header when proxying to the backend.
# PROXY_AUTH_SECRET=

# ══════════════════════════════════════════════════════════════════════
# Platform: Jira (used by team-tracker)
# ══════════════════════════════════════════════════════════════════════

# The Node API server must reach JIRA_HOST (DNS + HTTPS). ENOTFOUND usually
# means no DNS/VPN/offline from the machine running dev-server, not a bad token.
JIRA_EMAIL=you@redhat.com
JIRA_TOKEN=your-jira-api-token

# ══════════════════════════════════════════════════════════════════════
# Platform: GitHub (used by team-tracker)
# ══════════════════════════════════════════════════════════════════════

# Option 1: GitHub App (preferred for production)
# GITHUB_APP_ID=3263553
# GITHUB_APP_INSTALLATION_ID=121149773
# GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
# Or from file: GITHUB_APP_PRIVATE_KEY_FILE=./secrets/github-app-private-key.pem

# Option 2: Classic PAT (simpler for local dev)
# Classic PAT with read:user scope — fine-grained tokens don't work with GraphQL
GITHUB_TOKEN=your-github-pat

# ══════════════════════════════════════════════════════════════════════
# Platform: GitLab (used by team-tracker)
# ══════════════════════════════════════════════════════════════════════

# Configure instances via the Settings UI. Each instance references a token env var by name.
# GITLAB_TOKEN=           # Default token for GitLab.com
# GITLAB_BASE_URL=        # Optional legacy setting, defaults to https://gitlab.com (prefer Settings UI)
# GITLAB_TOKEN_INTERNAL=  # Example: token for an additional internal GitLab instance

# ══════════════════════════════════════════════════════════════════════
# Platform: IPA LDAP (used by team-tracker — requires VPN)
# ══════════════════════════════════════════════════════════════════════

# IPA_BIND_DN=uid=svc-account,cn=users,cn=accounts,dc=ipa,dc=redhat,dc=com
# IPA_BIND_PASSWORD=

# ══════════════════════════════════════════════════════════════════════
# Platform: Google (used by team-tracker — roster sync)
# ══════════════════════════════════════════════════════════════════════

# GOOGLE_SERVICE_ACCOUNT_KEY_FILE=./secrets/google-sa-key.json

# ══════════════════════════════════════════════════════════════════════
# Module: team-tracker
# ══════════════════════════════════════════════════════════════════════

# GitLab token for internal documentation instance (read_api scope)
# GITLAB_CEE_REDHAT_DOCS_TOKEN=

